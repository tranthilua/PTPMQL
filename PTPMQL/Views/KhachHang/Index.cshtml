@model IEnumerable<PTPMQL.Models.KhachHang>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Thêm mới khách hàng", "Create")
</p>
<table class="table">
    <tr>
        <th>
            <lablel>Mã Khách Hàng </lablel>
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TenKhachHang)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DiaChi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.KhachHangID)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.TenKhachHang)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.DiaChi)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.KhachHangID }) |
        @Html.ActionLink("Details", "Details", new { id = item.KhachHangID }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.KhachHangID })
    </td>
</tr>
}

</table>
